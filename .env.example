# App Environment
PORT=3000
NODE_ENV=development
APP_NAME=TilecraftInteriors
API_PREFIX=api/v1

# Database Configuration (SQLite default for dev, easily switched to PostgreSQL)
DATABASE_URL="file:./dev.db"

# JWT Secret
JWT_SECRET=tilecraft_super_secret_jwt_key_2026
JWT_EXPIRATION=7d

# Redis Configuration (Optional)
REDIS_HOST=localhost
REDIS_PORT=6379

# Uploads Configuration
UPLOAD_DIR=./uploads
