{
  "name": "tilecraftinteriors-backend",
  "version": "1.0.0",
  "description": "NestJS Backend for Tilecraft Interiors - Premium Interior & Stone Crafting Platform",
  "author": "Tilecraft Interiors Team",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "npx prisma generate && npx prisma db push && npm --prefix client install --include=dev && npm --prefix client run build && nest build",
    "build:client": "npm --prefix client install --include=dev && npm --prefix client run build",
    "build:all": "npx prisma generate && npx prisma db push && npm --prefix client install --include=dev && npm --prefix client run build && nest build",
    "render-build": "npm install --include=dev && npx prisma generate && npx prisma db push && npm --prefix client install --include=dev && npm --prefix client run build && nest build",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
    "start": "nest start",
    "start:dev": "nest start --watch",
    "start:debug": "nest start --debug --watch",
    "start:prod": "node dist/main",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "prisma:seed": "ts-node prisma/seed.ts"
  },
  "dependencies": {
    "@nestjs/cli": "^10.3.0",
    "@nestjs/common": "^10.3.0",
    "@nestjs/config": "^3.1.1",
    "@nestjs/core": "^10.3.0",
    "@nestjs/jwt": "^10.2.0",
    "@nestjs/passport": "^10.0.3",
    "@nestjs/platform-express": "^10.3.0",
    "@nestjs/schematics": "^10.1.0",
    "@nestjs/swagger": "^7.2.0",
    "@prisma/client": "^5.9.1",
    "@types/bcrypt": "^5.0.2",
    "@types/express": "^4.17.21",
    "@types/multer": "^1.4.11",
    "@types/node": "^20.11.16",
    "@types/nodemailer": "^8.0.1",
    "@types/passport-jwt": "^4.0.1",
    "bcrypt": "^5.1.1",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "nodemailer": "^9.0.5",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1",
    "prisma": "^5.9.1",
    "reflect-metadata": "^0.2.1",
    "rxjs": "^7.8.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@nestjs/testing": "^10.3.0",
    "@types/supertest": "^6.0.2",
    "source-map-support": "^0.5.21",
    "ts-loader": "^9.5.1",
    "tsconfig-paths": "^4.2.0"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  }
}
